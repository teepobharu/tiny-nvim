# Pending Task: Code ref helper polish

- [ ] Consider consolidating code-ref keymap mnemonics if clashes emerge; current prefixes: `<localleader>acr/acs/aca/ach` (relative) and `acR/acS/acA/acH` (absolute).
- [ ] Add which-key labels/grouping for the `ac` prefix.
- [ ] Decide whether to keep `refAbs` LuaSnip helper or replace with command-driven flow.
- [ ] Add tests or quick sanity checks for `utils.code_ref.current` when git root is missing.
- [ ] Document the workflow in docs/memory or README snippet section.
